{
  "name": "sagnik-sen-portfolio",
  "version": "2.0.0",
  "private": true,
  "description": "Accessible static portfolio with a source-backed GitHub project gallery",
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "test": "node --test tests/*.test.mjs",
    "dev": "node scripts/serve.mjs",
    "sync:github": "node scripts/sync-github.mjs"
  }
}
